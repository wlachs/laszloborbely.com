import * as React from 'react';
import Frame from '../components/Frame';

export default function Contact(props) {
    return (
        <Frame>
            <div>Contact</div>
        </Frame>
    );
};
