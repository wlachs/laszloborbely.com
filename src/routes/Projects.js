import * as React from 'react';
import Frame from '../components/Frame';

export default function Projects(props) {
    return (
        <Frame>
            <div>Projects</div>
        </Frame>
    );
};
