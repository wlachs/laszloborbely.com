import * as React from 'react';
import Frame from '../components/Frame';

export default function Welcome(props) {
    return (
        <Frame>
            <div>Welcome</div>
        </Frame>
    );
};
